package com.collection;

public class NeedOfCollection {
    public static void main(String[] args) {
        // Limitation of arrays
//          1. Array has a fixed size, one we fixed not able to change an array size.
//          2. Array has allowed only homogeneous data, not allowing heterogeneous data.
//          3. Array does not support an advance data structure like sort().
        // Overcome array limitation to introduce a Java collection framework.
    }
}
